# 本地开发模板，启动前复制为 .env 使用；仅供本机/测试环境，勿提交真实密码
# 如需无密码 Redis 调试，可将 SPRING_DATA_REDIS_PASSWORD 留空

SPRING_APPLICATION_NAME=hello         # 应用名，可自定义
SPRING_PROFILES_ACTIVE=dev            # 本地使用 dev 配置

# Redis 本地连接配置
SPRING_DATA_REDIS_HOST=localhost      # 本地 Redis 主机
SPRING_DATA_REDIS_PORT=6379           # 本地 Redis 端口
SPRING_DATA_REDIS_PASSWORD=changeme   # 本地测试密码，生产不要使用
SPRING_DATA_REDIS_TIMEOUT=2s          # 连接超时时间

# 服务暴露配置：若端口占用，可调整 SERVER_PORT
SERVER_PORT=8080
SERVER_ADDRESS=0.0.0.0
