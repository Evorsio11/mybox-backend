# 生产/CI 环境模板（值由 GitHub Environment 的 Variables/Secrets 注入）
# 操作指南：
# 1) 非敏感配置放 Environment Variables；敏感值放 Environment Secrets。
# 2) 工作流会读取 ${{ vars.* }} 和 ${{ secrets.* }} 写入 .env，请保持此模板键名不改。

SPRING_APPLICATION_NAME=hello             # 应用名，通常保持与服务名一致
SPRING_PROFILES_ACTIVE=prod               # 强制使用 prod 配置

# Redis 连接信息：host/port/timeout 可放 Environment Variables；密码放 Environment Secret
SPRING_DATA_REDIS_HOST=redis              # docker compose 内部服务名
SPRING_DATA_REDIS_PORT=6379               # Redis 端口
SPRING_DATA_REDIS_PASSWORD=               # 留空占位；由 secrets.PROD_REDIS_PASSWORD 覆盖
SPRING_DATA_REDIS_TIMEOUT=2s              # 连接超时时间，按需调整

# 服务暴露配置：端口需与 compose.yml 中映射保持一致
SERVER_PORT=8080                         # 容器内服务端口
SERVER_ADDRESS=0.0.0.0                   # 监听所有网卡
